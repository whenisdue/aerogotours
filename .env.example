RESEND_API_KEY=
INQUIRY_TO_EMAIL=
INQUIRY_FROM_EMAIL=

# Phase 2 private trip access (server-only; never prefix with VITE_)
# These two names are generated by the Neon Vercel integration.
TRIP_DATABASE_DATABASE_URL=
TRIP_DATABASE_DATABASE_URL_UNPOOLED=
# Optional legacy/local fallback when the Neon integration is not being used:
# TRIP_DATABASE_URL=
TRIP_ACCESS_HASH_SALT=
TRIP_BASE_URL=https://your-preview.vercel.app
# Fictional local demo only. Do not use this value for real trips.
TRIP_DEMO_PIN=2468
